<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Information</title>
</head>
<body>
  <a href="/attandance2">Back to Attendance</a>
  <h1>Student Information</h1>
  <p>Name: <%= student.fullname %></p>
  <p>SID: <%= student.sid %></p>
  <p>Roll No.: <%= student.roll %></p>
  <p>Class: <%= student.classs %></p>
  <p>Email: <%= student.email %></p>
  
  <% if (attendance) { %>
    <h2>Attendance Status</h2>
    <p>Status: <%= attendance.status %></p>
    <p>Check-In Time: <%= attendance.checkInTime.toLocaleTimeString() %></p>
    <p>Check-Out Time: <%= attendance.checkOutTime ? attendance.checkOutTime.toLocaleTimeString() : 'Not checked out yet' %></p>
    
    <% if (attendance.checkedOut && totalTime) { %>
      <h2>Total Time Spent in Lab</h2>
      <p><%= totalTime.hours %> hours and <%= totalTime.minutes %> minute</p>
    <% } else if (attendance.checkedOut) { %>
      <p>Total time not available.</p>
    <% } %>
    
  <% } else { %>
    <h2>Attendance Status</h2>
    <p>Status: Absent</p>
  <% } %>

  <% if (message) { %>
    <p><strong><%= message %></strong></p>
  <% } %>
  
</body>
</html>
